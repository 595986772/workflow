# Locked Experiment Evidence

`locked_snapshot_20260825/`是投稿前最终Pro复评时使用的关键结果快照，主要包含：

- 正式主结果与十seed统计；
- P3至P15相关协议锁、分析摘要和seed级CSV；
- 五类工作流、关键路径时延构成和缓存效果作图数据；
- 26000轮在线收敛曲线源数据；
- 服务器扩展、服务压力和固定预算容量异构性数据。

这些文件用于验证正文数字和重新生成论文图。目录中不包含模型checkpoint，也不代表所有历史实验；无效诊断结果和临时调参输出没有纳入。

公开入库时仅将旧记录中的机器专属绝对路径替换为`${PROJECT_ROOT}`或`${HOME}`，实验数值、配置、seed、哈希和统计结果均未改动。

解释数据时必须遵守对应协议边界：主结果使用十seed配对统计；容量异构性和服务压力实验只有三seed，只支持描述性趋势结论。
